import React from 'react';
import withResponsiveDrawer from './withResponsiveDrawer';

const MyTickets = () => {
  return <div>My Tickets Content</div>;
};

export default withResponsiveDrawer(MyTickets);