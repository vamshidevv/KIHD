import React from 'react';
import withResponsiveDrawer from './withResponsiveDrawer';

const SubmitTicket = () => {
  return <div>Submit Ticket Content</div>;
};

export default withResponsiveDrawer(SubmitTicket);